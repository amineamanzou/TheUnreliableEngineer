---
type Props = {
  src?: string;
  alt?: string;
  class?: string;
  loading?: "eager" | "lazy";
};

const {
  src = "/brand/web/characters/pointing_up.png",
  alt = "The Unreliable Engineer",
  class: className = "",
  loading = "lazy",
} = Astro.props;
---

<img
  class:list={["character-asset", className]}
  src={src}
  alt={alt}
  loading={loading}
  decoding="async"
/>
